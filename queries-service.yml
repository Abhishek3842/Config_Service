server:
  port: 8090

spring:
  application:
    name: queries-service

  datasource:
    url: jdbc:postgresql://localhost:5433/complaint_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka